{
    "name": "ionic-test",
    "version": "1.0.0",
    "description": "server for ionic",
    "main": "index.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "watch": "nodemon --ext js,pug --ignore public",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "lucio ticali",
    "license": "ISC",
    "dependencies": {
        "express": "^4.17.3",
        "http-server": "^14.1.0"
    },
    "devDependencies": {
        "connect-livereload": "^0.6.1",
        "livereload": "^0.9.3",
        "nodemon": "^2.0.15"
    }
}